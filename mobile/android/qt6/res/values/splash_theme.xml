<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light/default configuration -->
    <style name="Theme.AppSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
    <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo_themed</item>
        <item name="windowSplashScreenAnimationDuration">3000</item>

        <!-- Status bar and Nav bar configs -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/splash_background</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- After splash, use the light theme for default config; night variant overrides this -->
        <item name="postSplashScreenTheme">@style/LightTheme</item>
    </style>
</resources>
