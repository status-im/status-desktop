<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night configuration overrides to follow system dark theme -->
    <style name="Theme.AppSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
    <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo_themed</item>
        <item name="windowSplashScreenAnimationDuration">3000</item>

        <!-- Status bar and Nav bar configs -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- After splash, switch to dark theme in night mode -->
        <item name="postSplashScreenTheme">@style/DarkTheme</item>
    </style>
</resources>
